import { OrderItemPricePipe } from './order-item-price.pipe';

describe('OrderItemPricePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderItemPricePipe();
    expect(pipe).toBeTruthy();
  });
});
