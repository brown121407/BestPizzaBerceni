import { SpacerDirective } from './spacer.directive';

describe('SpacerDirective', () => {
  it('should create an instance', () => {
    const directive = new SpacerDirective();
    expect(directive).toBeTruthy();
  });
});
