import { PlainListDirective } from './plain-list.directive';

describe('PlainListDirective', () => {
  it('should create an instance', () => {
    const directive = new PlainListDirective();
    expect(directive).toBeTruthy();
  });
});
